# Planning Prompt Module

- Read `artifacts/extraction/<ticket-id>.json`.
- Create implementation plan with risks, assumptions, and validation checklist.
- Write `artifacts/plan/<ticket-id>.md`.
- Set approval state in `state/approval-status.json` with `approved: false`.
- Stop for human approval.
