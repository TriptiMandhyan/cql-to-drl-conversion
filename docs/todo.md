# TODO

- [ ] MCP-only intake enhancement: remove remaining HTTPS fallback by adding/supporting MCP retrieval of latest PR iteration changed CQL paths.
- [ ] After MCP PR-iteration coverage is available, remove `local-python.enrich_intake` tool usage from intake flow (underlying `ado_fallback.py` can also be retired).
