# TODO

- [ ] MCP-only intake enhancement: remove remaining HTTPS fallback by adding/supporting MCP retrieval of latest PR iteration changed CQL paths.
- [ ] After MCP PR-iteration coverage is available, remove `scripts/ado_fallback.py enrich-intake` usage from intake flow.
