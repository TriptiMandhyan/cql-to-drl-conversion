# TODO

- [ ] MCP-only extraction enhancement: remove remaining HTTPS fallback by adding/supporting MCP retrieval of raw CQL file content by repository + branch/commit + path.
- [ ] After MCP content retrieval is available, update extractor flow to run fully MCP-only when ticket provides repo, branch/commit, and CQL path.
