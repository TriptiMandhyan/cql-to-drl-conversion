# DRL Reference Examples

Place your approved example DRL files in this folder.

Conversion rules:
- Use these examples as the primary reference for rule naming style.
- Follow example patterns for shared-file handling and reusable utilities.
- Match formatting and coding conventions used by the examples.

This folder is intentionally empty by default; add one or more `.drl` files before running conversion for best results.
